---
import { CloudsparkAPIClient } from "@lib/cloudspark";

const subscriberId = Astro.params.subscriberId;
console.log("HI");
return await CloudsparkAPIClient(`/subscribers/${subscriberId}`, {
  method: "PUT",
});
---
